{
  "version": 2,
  "buildCommand": "pnpm run db:deploy && pnpm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "env": {
    "VERCEL_BUILD_SKIP_INSTALL": "0"
  }
} 