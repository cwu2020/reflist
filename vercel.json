{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm run db:deploy && npm run build",
        "outputDirectory": ".next"
      }
    }
  ],
  "env": {
    "VERCEL_BUILD_SKIP_INSTALL": "0"
  }
} 